Resently found bugs
1. Если перейти в другую вкладку, кроме профиля, система тебя разлогинивает, это не просто баг отображения, это действительно мешает пользователю(create my plan)
+ Copy template -система просит залогинться
2.Кнопка Create Custom exersise - не Enabled
!загрузка файла!
File file = new File("src/test/resources/qa-2-min (1).png");
driver.findElement(SELECT_FILE_BUTTON).sendKeys(file.getAbsolutePath());

